<% content_for :title, implementation_name(slug) %>

<h1 class="display-1"><%= implementation_name(slug) %></h1>

<ul class="list-group">
  <% implementation_versions(slug).each do |version| %>
    <li class="list-group-item">
      <%= link_to version, "/#{slug}/#{version}" %>
    </li>
  <% end %>
</ul>
